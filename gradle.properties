## Project-wide Gradle settings.
#
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
#
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx10248m -XX:MaxPermSize=256m
# org.gradle.jvmargs=-Xmx2048m -XX:MaxPermSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
#
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true
#Sat Mar 12 05:53:50 MSK 2016
APP_VERSION_CODE=6511
APP_VERSION_NAME=12.4.1
APP_PACKAGE=org.telegram.messenger
# MDGram: versión PROPIA del mod (fuente única para el updater y los releases). Subir en CADA release.
# El CI la puede sobreescribir con -PMDGRAM_VERSION_CODE=N -PMDGRAM_VERSION_NAME=VN.
MDGRAM_VERSION_CODE=5
MDGRAM_VERSION_NAME=V4.1
IS_PRIVATE=false
RELEASE_KEY_PASSWORD=android
RELEASE_KEY_ALIAS=androidkey
RELEASE_STORE_PASSWORD=android
# MDGram: máquina con 3.9 GB RAM total. -Xmx4096M causaba swapping/thrashing y muerte del daemon.
# Se baja el heap para que quepa en RAM (menos swap = builds más rápidos y estables) y se reduce
# la presión de memoria (parallel off en máquina RAM-limitada). Si hay OOM, subir a -Xmx2048m.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.daemon=true
org.gradle.parallel=false
org.gradle.configureondemand=true
kotlin.daemon.jvmargs=-Xmx512m
android.useAndroidX=true
android.enableJetifier=true
android.defaults.buildfeatures.buildconfig=true
android.nonTransitiveRClass=false
android.nonFinalResIds=false
android.enableR8.fullMode=true
